package main

import "fmt"

func main() {
    var num int

    fmt.Print("Digite um número inteiro: ")
    fmt.Scanln(&num)

    fatorial := 1

    for i := 1; i <= num; i++ {
        fatorial *= i
    }

    fmt.Println("O fatorial de", num, "é:", fatorial)
}
